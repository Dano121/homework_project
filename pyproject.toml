[project]
name = "lekcja8"
description = "Order ingestion pipeline: reads, cleans, validates and persists order data from CSV/JSON sources"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]